# 指定输入和输出的文件名
input_file = 'input.csv'
output_file = 'output.csv'
start_row = 0  # 开始行数（包含）
end_row = None  # 结束行数（不包含）

# 设置随机整数的上限和下限
lower_bound = 10
upper_bound = 100

# 设置要排除的数字列表
exclude_numbers = [15, 25, 35]
